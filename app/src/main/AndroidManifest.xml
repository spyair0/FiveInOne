<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.five">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".file">
        </activity>
        <activity android:name=".handler"></activity>
        <activity android:name=".hintintent" >
            <intent-filter>
                <action android:name="com.example.five.ACTION_START" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="acom.example.five.category_MYCATEGORY" />
                </intent-filter>
        </activity>
        <activity android:name=".chuandi1"/>
        <activity android:name=".chuandi2"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>